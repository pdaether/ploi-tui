version: "2"

linters:
  default: standard
  enable:
    - copyloopvar
    - misspell
    - unconvert
    - unparam

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/pdaether/ploi-tui
